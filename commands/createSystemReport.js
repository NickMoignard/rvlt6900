const createSystemReport = (msg, views = []) => {
    var returnString = "function stubbed (unfinished)";
    views.forEach((name) => {
        // get data from airtable
        returnString += ``;
    });
    msg.reply(returnString);
};
module.exports = createSystemReport;